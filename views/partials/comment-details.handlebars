<div class="comment">
  {{#each this}}
  <section class="comment-details">
    <div>
      {{comment_content}}
      <br><br> -- {{user.username}}, {{ format_date comment_date}}
    </div>
  </section>
  {{/each}}
</div>